document.querySelector('.shome').addEventListener('click', function(event) {
    event.preventDefault(); // Prevents the default behavior of the link
    window.location.href = 'index.html'; // Redirects to index.html
});
document.querySelector('.s-shop').addEventListener('click', function(event) {
    event.preventDefault(); // Prevents the default behavior of the link
    window.location.href = 'shop.html'; // Redirects to index.html
});
